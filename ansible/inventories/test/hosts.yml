---
all:
  children:
    testing:
      hosts:
        test-app-01:
          ansible_host: 10.0.2.10
        test-app-02:
          ansible_host: 10.0.2.11
        test-db-01:
          ansible_host: 10.0.2.20
        test-cache-01:
          ansible_host: 10.0.2.21
      vars:
        environment: testing
        deploy_path: /opt/enterprise-app
        docker_registry: registry.test.example.com
        app_port: 8000
        workers: 2
        enable_debug: false
        database_host: test-db-01
        redis_host: test-cache-01
        run_tests: true